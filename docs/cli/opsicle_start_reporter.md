## opsicle start reporter

Starts the reporter component

### Synopsis

Starts the reporter component which reports on the services

```
opsicle start reporter [flags]
```

### Options

```
      --healthcheck-interval duration   specifies the interval between healthcheck pings (default 3s)
  -h, --help                            help for reporter
      --listen-addr string              specifies the listen address of the server (default "0.0.0.0:11111")
      --mongo-host strings              Specifies the hostname(s) of the MongoDB instance (default [127.0.0.1:27017])
      --mongo-password string           Specifies the password to use to login to the MongoDB instance (default "password")
      --mongo-user string               Specifies the username to use to login to the MongoDB instance (default "opsicle")
      --mysql-database string           specifies the name of the central database schema (default "opsicle")
      --mysql-host string               specifies the hostname of the database (default "127.0.0.1")
      --mysql-password string           specifies the password to use to login (default "password")
      --mysql-port string               specifies the port which the database is listening on (default "3306")
      --mysql-user string               specifies the username to use to login (default "opsicle")
      --nats-addr string                Specifies the hostname (including port) of the NATS server (default "localhost:4222")
      --nats-nkey-value string          Specifies the nkey used to login to NATS (default "SUADZTA4VJHBCO7K75DQ3IN7KZGWHKEI26D2IYEABRN5TXXYHXLWNDYT4A")
      --nats-password string            Specifies the password used to login to NATS (default "password")
      --nats-username string            Specifies the username used to login to NATS (default "opsicle")
      --redis-addr string               defines the hostname (including port) of the redis server (default "localhost:6379")
      --redis-password string           defines the password used to login to redis (default "password")
      --redis-user string               defines the username used to login to redis (default "opsicle")
```

### Options inherited from parent commands

```
  -C, --config string      Defines the location of the global configuration used (default "~/.opsicle/config")
  -l, --log-level string   Sets the log level (one of [trace, debug, info, warn, error]) (default "info")
  -o, --output string      Sets the output format where applicable (one of [text, json]) (default "text")
```

### SEE ALSO

* [opsicle start](cli/opsicle_start.md)	 - Starts one of Opsicle's core services

###### Auto generated by spf13/cobra on 30-Nov-2025
